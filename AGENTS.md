# 仓库工作约定

任何修改前依次阅读：

1. `README.md`
2. `备赛/PROJECT_CONTEXT.md`
3. `备赛/00_赛事基线.md`
4. 当前题目目录中的 `requirements.md`、`pinmap.yaml` 和 `verification.md`

必须遵守：

- `archive/` 默认为只读；修订旧项目只能新建说明，不改封存内容。
- 不猜引脚、电平、定时器、器件地址、极性、量程或正式规则。
- 每道题独立放在 `problems/<日期>-<代号>/`，不得把题目逻辑塞进通用层。
- 固件上电时执行器、激光和功率级默认关闭；通信必须有超时和失联安全状态。
- 测试结论必须带仪器、输入条件、原始数据路径、固件提交号和通过阈值。
- 变更协议、引脚、时钟或电源域时，必须同步文档和回归测试。
- 生成的 `Debug/`、二进制和原始长日志不直接入库；正式交付物按复现规范封存。
