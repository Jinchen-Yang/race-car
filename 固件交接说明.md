# 小车固件交接说明（MSPM0G3507）

> 给后续开发(学长学姐)的总纲。代码 = "AI 出骨架 + 人定物理量":驱动结构/SDK 调用已写好并带中文悬停注释,**所有控制参数/标定值是占位**,需现场整定。
> 配套:接线图 `diansai-skill/design/wiring.svg/png` + 接线表 `harness.md` + 引脚契约 `contracts/pinmap.yaml`。

---

## ★ 状态速览（2026-07-04 更新，比下文各节更新，冲突以此为准）

**已完成并真机验证：**
- SysConfig 11 实例全部建好(§3 已完成; 期间修掉一个坑: ENC_R_A 忘指定引脚被自动解算到 PA14, 已显式改回 PA27——**GPIO 引脚必须显式指定, 别信自动解算**)。
- `empty.c` 已是整合版: 全驱动 init + 7 任务调度 + NVIC 使能(SysConfig 生成代码不做 NVIC 级使能, main 里必须手动 `NVIC_EnableIRQ`, 这是 TI 的约定)。
- 双编码器已标定: 左 1017.4 计数/圈(QEI 4倍频)、右 265.2(单相1倍频), 见 encoder.h; 轮径粗测 50mm。
- **速度内环已闭环成功**: 目标 300mm/s, 实测贴住, 稳态占空≈150 与开环标定(占空200→约400mm/s, G≈2.0)吻合。当前 Kp=0.15/Ki=0.03。
- 感为灰度 ganv003 协议已按官方手册填真值: 地址 0x4C(按无跳线帽; AD0/AD1 装帽要改)、命令 0xDD 读数字量、压黑线=bit0、init 内含 ping(0xAA→0x66)在线检测。
- 修复过的坑(勿回退): motor_set 每次置位 STBY(否则 IDLE 态 stop_all 把 TB6612 闩死在待机); 测速用 g_tick_ms 真实 dt; BSP_I2C_TIMEOUT_LOOPS=10000; scheduler.h 的 timer/run 已加 volatile。
- VOFA 12 通道: 0=状态机 1/2=左右计数 3=K230数据年龄 4/5=左目标/实测 6/7=右目标/实测 8/9=左右占空 10=灰度偏差 11=丢线标志。波特率 115200。

**还没做/待整定：** 灰度真机联调(ping 判在线)与位序/极性校验、循迹外环整定、丢线盲走 HEADING_KP、reached_keypoint(恒0,不会自动停车)、瞄准几何、舵机/K230/激光未接、IMU 零漂校准未在 boot 调用、带载(地面)后速度环增益复整。

---

## 1. 分层架构

```
app(应用)        app.c/.h ── 速度内环PID + 循迹外环PID + 状态机(骨架,逻辑待填)
   │
middleware(中间件) pid / scheduler(时间片) / vofa(波形回传)  ── 已写实,可直接用
   │
driver(驱动)      motor 编码器 servo imu(mpu6050) gray(感为灰度) hmi(蜂鸣/灯/按键) k230
   │
bsp(底层)         bsp_i2c(I2C读写寄存器封装)  ── 其余驱动直接调 SDK DriverLib
   │
SDK              TI MSPM0 SDK 2.10 DriverLib(DL_*) + SysConfig 生成的 ti_msp_dl_config
```

**悬停看注释**:每个对外函数有 `@brief/@param/@return` 中文 Doxygen,变量有 `/**< */`——VSCode(装 C/C++ 插件)或 CCS 鼠标悬停即显示。

## 2. 文件清单

| 文件 | 作用 | 状态 |
|---|---|---|
| `motor.c/.h` | 电机(TB6612,PWM+方向+STBY,占空封顶60%) | ✅ 已实测 |
| `encoder.c/.h` | 左轮 TIMG8 硬件QEI + 右轮 GPIO中断 | 结构完成,系数待标定 |
| `servo.c/.h` | 云台 pan/tilt(TIMA1 50Hz)+ 舵机轨EN | 结构完成,脉宽端点待标定 |
| `mpu6050.c/.h` | IMU(I2C 0x68),航向角(陀螺积分) | 结构完成,零漂/融合待整 |
| `gray.c/.h` | 感为8路灰度(I2C)+ 加权质心求偏差 | ⚠ I2C地址/寄存器待感为协议 |
| `bsp_i2c.c/.h` | I2C1 读写寄存器封装(imu/gray 共用) | ✅ 已对 SDK 核 API |
| `hmi.c/.h` | 蜂鸣(低触发)+ 状态灯 + 按键(消抖) | 结构完成 |
| `k230.c/.h` | UART2 收 K230 视觉帧(非阻塞,协议同源) | 结构完成 |
| `app.c/.h` | 控制环 + 状态机 骨架 | ⚠ 控制逻辑/几何/整定 待填 |
| `pid/scheduler/vofa` | 中间件 | ✅ 可用 |
| `protocol.h` | K230↔MCU 帧协议(与 contracts/protocol.h 同源,勿手改) | ✅ |
| `empty.c` | 当前是**电机开环 bring-up** 主程序 | 待按 §4 换成整合版 |

## 3. ★ SysConfig 要新建的实例（不建则编译不过）

打开 `empty.syscfg`,**新增**下列实例(命名必须**逐字一致**,代码按这些宏名引用):

| 实例名 | 外设 | 引脚/设置 |
|---|---|---|
| `I2C_BUS` | I2C1(主机) | SCL=PB2, SDA=PB3,100kHz,**不开中断**;板上 SCL/SDA 外部上拉 **3.3V** |
| `PWM_SERVO` | TIMA1(50Hz/20ms) | CC0=PA17(PAN),CC1=PA16(TILT);**Start Timer 不勾**(servo_init 启动) |
| `QEI_ENC_L` | TIMG8(QEI 2输入) | PHA→CCP0=PA29,PHB→CCP1=PA30;Period/LOAD=0xFFFF |
| `UART_K230` | UART2(115200 8N1) | TX=PB17,RX=PB16;**★必须勾 RX 接收中断** |
| `GPIO_IOA` | GPIOA 组 | KEY_MODE=PA22(输入+上拉) · LED_RUN=PA24(输出,初值Low) · LED_ERR=PA26(输出,Low) · ENC_R_A=PA27(输入,**上升沿中断**,使能 GPIOA 组 NVIC) |
| `GPIO_IOB` | GPIOB 组 | BUZZER=PB18(输出,**初值High=静音**) · KEY_START=PB19(输入+上拉) · SERVO_EN=PB23(输出,**初值Low=断电**) · ENC_R_B=PB24(输入) |

**已存在、勿重建**:`PWM_MOTOR`(TIMA0 PA8/PB9)、`MOTOR_A`(PA12/PA13)、`MOTOR_B`(PB6/PB7/PB0)、`UART_VOFA`(UART0)、`GPIO_LED`(PB26 心跳)、SYSTICK。

> ⚠ `ENC_R_A`(PA27)与 hmi 的灯/键同在 GPIOA 组:建组时只给 PA27 开中断,其它脚不开;encoder 的 GPIOA 中断处理里要**按位判 PA27** 再处理,清中断只清该位。

## 4. 接进 main 的方法（peripherals + SysConfig 就绪后）

当前 `empty.c` 是电机开环 bring-up 版。整合时,在 `main()` 的 `SYSCFG_DL_init();` 之后依次:
```c
vofa_bind_writer(uart_write);
motor_init();  enc_init();  imu_init();  gray_init();
servo_init();  hmi_init();  k230_init();  app_init();
/* bsp_i2c 无需 init(I2C 由 SYSCFG_DL_init 配好) */
```
任务表 `g_tasks[]` 增加(周期按需整定):
```c
{ task_heartbeat, 500, 500, 0 },   /* led_heart_toggle */
{ task_key,        10,  10, 0 },   /* key_scan         */
{ task_k230,        5,   5, 0 },   /* k230_poll 推进收帧 */
{ task_vel,        10,  10, 0 },   /* vel_loop_step 速度内环 */
{ task_track,      20,  20, 0 },   /* track_loop_step 循迹外环 */
{ task_fsm,        20,  20, 0 },   /* app_fsm_step 状态机 */
{ task_vofa,       20,  20, 0 },   /* 调试波形 */
```
> 中断优先级建议:控制环 tick(SysTick) > 串口/编码器中断 > VOFA;急停 app_estop() 可在按键中断里调。

## 5. ★ 待填清单（占位/标定/确认）—— 搜代码里的 `TODO` 和 `待整定`

**协议(阻塞 gray)**
- [ ] 感为8路灰度 I2C **地址 + 寄存器**:填 `gray.c` 的 `GRAY_I2C_ADDR` / `GRAY_REG_DIGITAL` + `gray_read_raw()` 的解析格式(待感为 datasheet/wiki)。

**标定(真车测)**
- [ ] 编码器:每圈计数(手转一圈数计数)、左右方向符号、`ENC_L_COUNT_MODULO`。
- [ ] 速度内环 PID:`Kp/Ki`、输出限幅(先内环铁律)。
- [ ] 循迹外环 PID:`Kp/Kd`;灰度权重/阈值/中线零点;8路"物理左→右 与 数组下标"对应。
- [ ] 舵机:脉宽端点(0.5~2.5ms 占位)、角度限位、TILT 中位。
- [ ] 几何瞄准 `aim_geometry()`:由里程/航向反解 pan/tilt 的公式(需场地几何:ABCD 位置/朝向、靶心规格——题目留白,对官方 PDF 核)。
- [ ] 关键点判定 `reached_keypoint()`、丢线盲走(IMU 航向锁)。
- [ ] K230:波特率、帧超时、dx/dy 正方向(与算法 lane 对齐)。

**硬件确认**
- [ ] I2C:SCL/SDA **4.7kΩ 上拉到 3.3V**;感为 +5V 供电时确认 I2C 电平≤3.3V(关感为 PULL 跳线);感为 AD0/AD1 设地址 ≠0x68。
- [ ] 舵机:独立 5~6V 电源轨,EN(PB23)控制。
- [ ] STBY(PB0):成品板若无内置下拉,外接 10k 到 GND。
- [ ] 激光:**未采购**,买后定 1 个 GPIO 控开关(代码留 `laser_on/off` 接口位)或常亮。
- [ ] MPU6050:实测 WHO_AM_I 值(0x68/0x70/0x71 视片)。

## 6. 引脚/接线

以 `diansai-skill/contracts/pinmap.yaml`(已过 pinmux_check)为唯一真值;图见 `design/wiring.svg`(模块卡片版)/`wiring.png`(Graphviz)/`harness.md`(接线表)。车头=万向轮+灰度,车尾=两驱动轮。

## 7. 环境/编译

CCS Theia + MSPM0 SDK 2.10,编译器变体 **ticlang**。所有 .c/.h 平铺在工程目录(CCS 按目录收源)。先建齐 §3 的 SysConfig 实例并保存生成,再编译。
