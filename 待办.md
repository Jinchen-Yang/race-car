# 待办事项

## 硬件整定任务（r35 代码改动后需要现场操作）

### 1. 测量轮距（影响互补滤波精度）
- 让车原地转 3 圈，记录 IMU 累计转角 Δyaw_enc 和右轮里程 Δs
- 反算：`track = Δs × 360 / (Δyaw_enc × π)`
- 回填到 `mpu6050.c` 的 `IMU_WHEEL_TRACK_MM`（当前估值 140mm）

### 2. 整定弧线段质心换算系数 `ARC_ERR_TO_DEG`
- 烧录后在弧线上跑，用串口 `'P'/'p'` 调 `g_kp_arc`
- 观察弧上循线是否平滑
- 弧上纠偏太猛(抖) → 调小 `ARC_ERR_TO_DEG`
- 弧上纠偏太弱(跑偏) → 调大 `ARC_ERR_TO_DEG`
- 当前默认值：0.12

### 3. 整定互补滤波系数 `IMU_FUSION_ALPHA`
- 跑一圈后看 VOFA ch12(航向角)
- 回 A 时航向漂移仍大（>5°）→ 调小 alpha 到 0.95（更重编码器）
- 航向出现高频抖动 → 调大到 0.99
- 当前默认值：0.98

### 4. 验证 ESTOP 慢闪
- 运行中按 MODE 键触发急停
- 确认红灯是慢闪（500ms亮/500ms灭）而非常亮
- 2 秒内连按 3 次 START 确认清障回 IDLE

### 5. 验证 START 跳过校准
- 断电重启
- 串口看到 "calibrating... press START to skip" 时按 START 键
- 确认能跳过校准直接进 IDLE

### 6. 修编码器硬件（速度闭环前置条件）
- 检查左编码器接线（PA29/PA30 的 QEI 输入是否虚焊）
- 右编码器 PA27 加硬件 RC 滤波（100Ω+100pF 到地）或软件中值滤波
- 确认编码器供电（5V 还是 3.3V？与 MCU IO 电平是否匹配？）
- 修好后把 `app.c` 的 `DRIVE_OPEN_LOOP` 改回 0 启用速度闭环