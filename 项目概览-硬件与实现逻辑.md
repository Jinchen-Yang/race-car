# race-car 项目概览 — 硬件与实现逻辑

> 北邮 2026 电赛小车赛道 · 固件基准 r32 · 本文为对外分享版（完整工程见 GitHub: Jinchen-Yang/race-car）
> 任务：220×120cm 场地，双 R=40cm 半圆弧黑线 + 空白直线段；自动巡迹一圈(F1 ≤30s)、定点瞄准(F2 ≤5s)、巡迹+对靶联动(F3 ≤40s)、四圈连跑(发挥)。当前：循迹全线已通（单圈 ≈20s 真机验收），瞄准标定进行中。

---

## 一、硬件清单

| 部件 | 型号 | 关键参数 | 接口/备注 |
|---|---|---|---|
| 主控 | TI LP-MSPM0G3507 LaunchPad | Cortex-M0+ @32MHz（上限 80MHz），SysTick 1ms 时基 | 调试串口走板载 XDS110 |
| 电机驱动 | 2× TB6612FNG（成品四路板） | VM 工作上限 13.5V（**仅限 3S 锂电直供**）；PWM 20kHz；IN 定方向+PWM 斩波（short-brake 慢衰减，低速线性最好） | TIMA0 双路 PWM + 方向 GPIO + STBY |
| 电机+编码器 | MG310 直流减速电机 ×2 | 12V，霍尔 13PPR，减速比 ≈1:20；输出轴每圈计数：左 1017.4（QEI 4 倍频）/ 右 254.35（GPIO 1 倍频） | 左 TIMG8 硬件 QEI / 右 GPIO 上升沿中断+B 相判向 |
| 灰度传感器 | 感为科技 8 路 I2C 版 Rev V3.6 | 内部刷新 ≈1.5kHz，探头响应 1.5ms；数字量 1 字节（bit=0 压线），地址 0x4C | I2C1 @100kHz，独占总线 |
| IMU | MPU6050（GY-521） | 陀螺 ±250dps / 131 LSB/(°/s)，DLPF 42Hz，采样 100Hz，PLL 时钟源；上电校准零偏（丢 100 帧+平均 500 帧） | I2C0 @100kHz（与灰度分总线，防从机互扰） |
| 云台 | PAN 180° 位置舵机（MG996R 类）| 50Hz PWM，脉宽端点待实测标定；TILT 机械固定 ≈26° 上仰 | TIMA1；舵机轨可开关（巡线模式全程断电） |
| 视觉(选配) | 嘉楠 K230（CanMV） | 640×480 find_blobs ≈30fps，UART 115200 每帧 11 字节；主控 200ms 无帧自动回退纯几何瞄准 | UART2，拔掉不影响 F1~F3 |
| 遥测 | VOFA+ JustFloat 24 通道 | 50ms/帧（r32），115200 | UART0/XDS110；真场地可用 ESP32 蓝牙桥或 OpenLog SD 黑匣子并联 TX |

**供电拓扑**：电机/驱动板 = 3S 锂电 ~12V；主控+灰度 = USB/充电宝 5V（buck 割接待做）；舵机轨 = 驱动板 5V。红线：灰度与舵机/电机等感性负载**严禁共 5V 轨**（厂商强制条款）；`MOTOR_MAX_DUTY=600`（60% 占空）是 7.4V 电机对 12V 电池的防超压约束。

## 二、固件架构（CTY 式，r25 起）

```
SysTick 1ms 置就绪标志 ── 主循环任务表轮询（run-to-completion，3 个 ISR 全部最小化）
├─ track_loop_step(20ms)  转向核：一切误差统一折算成 DeltaYaw(°) → PD → 左右差速占空
│    ├─ 弧线段: 灰度 8 位查表(±5/15/25/40°) + 丢线保护窗强拐搜索
│    ├─ 空白段: 绝对航向保持(段0 目标=z, 段2 目标=z+180; z=START 时刻车头方向快照)
│    └─ 四段状态机: 压线/离线事件驱动(漏积分计数认线), 一圈 4 节点 B/C/D/A
├─ vel_loop_step(10ms)    编码器测速仅遥测(r20 起速度闭环弃用, 行驶=开环占空)
├─ app_fsm_step(10ms)     IDLE/RUN/AIM/STOP/ESTOP; 节点声光/模式路由/四圈计圈
├─ task_imu(10ms)         MPU6050 读取+真实 dt 积分 yaw(唯一推进点)
└─ task_vofa(50ms)/task_cmd  24 通道遥测 + 串口命令台(在线调参, 断电即失)
```

## 三、控制实现逻辑（核心思想：单一误差接口）

1. **DeltaYaw 统一接口**：无论误差来自灰度查表（弧线）、绝对航向差（直线）还是强拐搜索（丢线），都折算成"车头应转多少度"，喂同一个 PD → 差速占空。好处：整定只有一组增益语义，段间切换不换控制器。
2. **SelfTurn PD（r32 增益分家）**：直线航向 KP=6.0（定版，压稳态残差）；弧线查表 KP_ARC=3.5（避免把 ±5/15/25/40° 查表台阶放大成极限环=蛇形）；KD=2.0 抑制台阶跳变动量；段切换时清 D 历史（防换源单拍甩头）。输出：先夹差速 ±150，再叠加基速 150 占空，硬限幅 600。
3. **开环行驶 + 配平**：r20 起弃用速度闭环（台架实锤两个编码器带病：左轮间歇失联、右轮 ±20% 电气毛刺），改开环占空 + 右轮配平常数 + 陀螺航向闭环兜底。代价：对电池电压敏感（速度 ∝ Vbat），满电整定的配平在亏电时失配。
4. **四段状态机 + 漏积分认线**：空白帧计数自增（封顶）、有线帧减半——"快认线/慢认空"，单帧褶皱/阴影免疫；认线门限 8（≈5 帧 @300mm/s），曾调松到 63 引发假事件链式推进事故后回退定版。r32 起 I2C 失败帧显式判"无数据"，不喂计数器和查表（堵"冻结字节拐死/段误切"缺陷）。
5. **IMU 航向体系**：上电校准零偏（静止），yaw 用真实 dt 积分（I2C 超时拖延调度时名义周期失真 2~3 倍，实测定案）；模式 4 每圈过 A 用限幅 ±10°/圈 的重锚吸收陀螺漂移（整锚替换曾把出线姿态噪声全额注入下一圈，已改夹幅修正）。
6. **起步保护**：START 后 1.5s 强制段 0 + 无视灰度（A 点残端/起步抖动免疫），摆车须离胶带 ≥50cm。
7. **K230 瞄准链路（选配）**：K230 跑 MicroPython 黑阈值找靶心，每帧发 `AA 55 06 05 dxL dxH dyL dyH found CHK 0D`；主控 UART 中断收进环形缓冲，5ms 消费；200ms 无帧回退纯几何解算（场地常量实测：AB=1000 / 靶投影 530 / 靶心高 500 / 舵机轴高 161.5mm）。
8. **工程可靠性**：I2C 双总线隔离 + 9 时钟+STOP 总线自愈 + 3ms 有界超时；急停=运动中任意键（ESTOP 锁存，2s 内三连 START 软清障）；版本水印制度（boot 横幅 rN，防"烧了个寂寞"）；每轮改动一条 commit（现象→机理→修复）。

## 四、定版参数（真机整定）

| 参数 | 值 | 含义 |
|---|---|---|
| TRACK_KP / KP_ARC / KD | 6.0 / 3.5 / 2.0 | 直线航向 KP / 弧线查表 KP（r32 分家）/ 微分 |
| BASE_SPEED | 150 占空 | ≈300mm/s 巡航 |
| SEG_LINE_TH / SEG_BLANK_TH | 8 / 16 | 认线 ≈5 帧 / 认空白 ≈16 帧（勿轻动） |
| START_PROTECT_MS | 1500 | 起步保护窗 |
| 每圈重锚限幅 | ±10° | 模式 4 专用 |
| MOTOR_MAX_DUTY | 600 | 电机防超压硬限幅（禁提高） |

## 五、当前状态与已知问题

- ✅ F1 循迹全线真机通过（≈20s/圈）；模式/急停/遥测/命令台体系完备。
- 🔧 进行中：瞄准标定（云台中位需装成指左，解算角 +133°）；供电 5V buck 割接；"首跑顺畅后续跑偏"排查中——头号嫌疑=IMU 零偏温漂/校准时机（boot 唯一校准），判别实验与修复方案见仓库 issue #2/#3 与《硬件参数与采样率审计.md》。
- 📋 完整调试史：git log 每条 commit 含现象→机理→修复。
